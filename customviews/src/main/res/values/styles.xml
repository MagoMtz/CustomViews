<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SpinnerStyle" >
        <item name="android:textAppearance">?android:attr/textAppearanceListItemSmall</item>
        <item name="android:gravity">center_vertical</item>
        <item name="paddingStart">?android:attr/listPreferredItemPaddingStart</item>
        <item name="paddingEnd">?android:attr/listPreferredItemPaddingEnd</item>
        <item name="android:minHeight">?android:attr/listPreferredItemHeightSmall</item>
    </style>

    <style name="HintText" >
        <item name="android:textSize">@dimen/hint_text_size</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="HintErrorText" >
        <item name="android:textSize">@dimen/hint_text_size</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="TextInputLayoutAppearance">
        <item name="hintTextAppearance">@style/HintText</item>
        <item name="errorTextAppearance">@style/HintErrorText</item>
    </style>

    <style name="EditTextHintAppearance" >
        <item name="android:textSize">@dimen/hint_text_size</item>
        <item name="android:layout_marginStart">@dimen/hint_margin_start</item>
    </style>

</resources>
